import Link from "next/link";
export default async function OrderSuccess({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; return <main className="mx-auto max-w-xl px-5 py-20 text-center"><p className="text-sm font-semibold text-accent">ORDER CONFIRMED</p><h1 className="mt-2 text-4xl font-semibold">Thank you for your order.</h1><p className="mt-4 text-slate-600">Your order reference is <span className="font-mono">{id}</span>.</p><Link href="/products" className="mt-8 inline-block rounded bg-ink px-5 py-3 font-semibold text-white">Continue shopping</Link></main>; }
