import { loginAction, registerAction } from "@/app/actions";

export function AuthForm({ mode }: { mode: "login" | "register" }) {
  const action = mode === "login" ? loginAction : registerAction;
  return <form action={action} data-agentbridge-form={mode} className="space-y-4 rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><h1 className="text-2xl font-semibold">{mode === "login" ? "Welcome back" : "Create your account"}</h1>{mode === "register" && <div className="grid grid-cols-2 gap-3"><label className="text-sm font-medium">First name<input required name="firstName" data-agentbridge-field="firstName" className="mt-1 w-full rounded border p-2" /></label><label className="text-sm font-medium">Last name<input required name="lastName" data-agentbridge-field="lastName" className="mt-1 w-full rounded border p-2" /></label></div>}<label className="block text-sm font-medium">Email<input required type="email" name="email" data-agentbridge-field="email" className="mt-1 w-full rounded border p-2" /></label><label className="block text-sm font-medium">Password<input required minLength={mode === "register" ? 8 : 1} type="password" name="password" data-agentbridge-field="password" className="mt-1 w-full rounded border p-2" /></label><button data-agentbridge-action={mode} className="w-full rounded bg-ink py-3 font-semibold text-white">{mode === "login" ? "Sign in" : "Create account"}</button></form>;
}
